export interface IStoreStatus {
  loading: boolean;
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  error: null | any;
}

export interface IBinaryNodeDefault {
  id: string;
  title: string;
}
