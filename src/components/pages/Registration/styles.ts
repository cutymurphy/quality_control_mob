import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    flex: 1,
    padding: 28,
  },
  fields: {
    paddingTop: 36,
    paddingBottom: 52,
    paddingHorizontal: 12,
    gap: 12,
  },
});
